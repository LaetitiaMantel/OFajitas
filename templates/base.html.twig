<!DOCTYPE html>
<html lang="fr"></html>
<html>
	<head>
		<meta charset="utf-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
		<meta name="description" content=""/>
		<meta name="author" content=""/>
		<title>
			{% block title %}O'Fajitas
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		{% block stylesheets %}

			<!-- Favicon-->
			<link
			rel="icon" type="image/x-icon" href="assets/favicon.ico"/>
			<!-- Bootstrap icons-->
			<link
			href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet"/>
			<!-- Core theme CSS (includes Bootstrap)-->
			<link href="{{ asset('css/styles.css') }}" rel="stylesheet"/>
		{% endblock %}
	</head>
	<body>
		<!-- Header-->
		{% if is_granted ("ROLE_MANAGER") %}
			<header></header>

		{% else %}
			<header class="gradient py-5">

				<div class="container px-4 px-lg-5">

					<div class="text-center text-white">

						<img class="img-fluid" src="{{ asset('images/fajitas2.png')}}" alt="fajitas" srcset="">
						<h1 class="lead fw-normal text-black-50 mb-0">Pour les Fans de Fajitas
						</h1>
					</div>
				</div>
			</header>
		{% endif %}

		<!-- Navigation-->
		{% if is_granted ("ROLE_MANAGER") %}
			{{ include('_navbar_back.html.twig') }}
		{% else %}
			{{ include('_navbar.html.twig') }}
		{% endif %}

		{% block body %}{% endblock %}
		
		<footer class=" gradient">
			<div class="row p-3 ">
				<div class="col-9 ">
					<a href="{{ path('front_main_contact') }}" class="link-secondary p-3">
						<i class=" bi bi-envelope ">
							Nous contacter</i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
					<a href="{{ path('front_main_qsn') }}" class="link-secondary ">
						<i class="bi bi-emoji-smile-upside-down-fill"></i>
							Qui sommes nous</i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
				</div>
				<div class="col-3 d-flex justify-content-end ">
				<div>
					<a href="#" class="btn btn-outline-dark mb-2">
						<i class="bi bi-facebook"></i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
					<a href="#" class="btn btn-outline-dark mb-2">
						<i class="bi bi-twitter"></i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
					<a href="#" class="btn btn-outline-dark mb-2">
						<i class="bi bi-instagram"></i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
					<a href="#" class="btn btn-outline-dark mb-2">
						<i class="bi bi-share"></i>
						<span id="cart-count" class="badge bg-dark text-white ms-1 rounded-pill" data-cart-count-url="{{ path('front_cart_get_cart_count') }}"></span>
					</a>
				</div>
				</div>
			</div>
			<div class="container text-center">
				<p class="m-0 text-center text-black-50">Copyright &copy; O'Fajitas 2024</p>
				<img class="w-10" src="{{ asset('images/powered-by-fajitas.png')}}" alt="fajitas" srcset="">
			</div>
		</footer>

		<!-- Bootstrap core JS-->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
		<!-- Core theme JS-->
		<script src="{{ asset('js/scripts.js') }}"></script>
		<script src="{{ asset('js/favoris.js') }}"></script>
		<script src="{{ asset('js/connecter.js') }}"></script>

		<script>
			const addToFavoriteUrl = "{{ path('front_favorite_add', {id: 1}) }}";
			const deleteFavoriteUrl = "{{ path('front_favorite_delete', {id: 1}) }}";
			const connecterUrl = "{{ path('front_user_connecter')}}";
		</script>


	</body>
</html>
