{% extends 'base.html.twig' %}

{% block title %}
	{{ user.firstname }}
{% endblock %}

{% block body %}
	<div class="container">
		<h2 class="h2-color">{{ user.firstname }} {{ user.lastname }}</h2>


		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ user.id }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ user.email }}</td>
				</tr>
				<tr>
					<th>Roles</th>
					<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
				</tr>
				<tr>
					<th>Password</th>
					<td>{{ user.password }}</td>
				</tr>
				<tr>
					<th>Firstname</th>
					<td>{{ user.firstname }}</td>
				</tr>
				<tr>
					<th>Lastname</th>
					<td>{{ user.lastname }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-success mb-2" href="{{ path('back_user_index') }}">Retour à la liste</a>

		<a class="btn btn-warning mb-2" href="{{ path('back_user_edit', {'id': user.id}) }}">Modifier</a>

		{{ include('back/user/_delete_form.html.twig') }}
     </div>
{% endblock %}
