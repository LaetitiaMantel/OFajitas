{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier l'utilisateur</h1>
	{% if (user.roles == 'ROLE_USER')%}
		{{ include('back/customer/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    {% else %}
        {{ include('back/user/_form.html.twig', {'button_label': 'Update'}) }}
	{% endif %}

		<a class="btn btn-success mb-2" href="{{ path('back_user_index') }}"> Retour à la liste </a>
    </div>

	
{% endblock %}
